# Tests

Passed before the full quality gate:

- MinimalCmsCoreBoundaryTest;
- RuntimeSecurityClusterSmokeTest;
- syntax checks for all changed runtime classes;
- direct upper-package import scan.
- full Core lint, static analysis and regression suite;
- metadata, evidence and scope gates after the bounded metadata correction.
