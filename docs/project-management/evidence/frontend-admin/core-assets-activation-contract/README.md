# Core Assets Activation Contract

This batch adds a contract-only owner boundary for future SIMAI Framework smart components resource pack activation.

It proves that `larena/core:core.assets` can receive asset requirement descriptors and produce a no-write activation plan without copying frontend runtime into the root app.

