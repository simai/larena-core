# Core evidence: Admin API v1

This packet covers only the generic optional transaction seam used by the
Larena Admin API v1 composition. HTTP, session, CSRF, schemas, rate limiting and
idempotency remain outside Core.

Status: implementation and package QA complete. The containing commit is the
exact Core revision consumed by the parent Admin API v1 release chain.
