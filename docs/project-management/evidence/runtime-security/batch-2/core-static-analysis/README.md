# Core Static Analysis Evidence

Batch: `runtime-security/batch-2/core-static-analysis`

Purpose: harden the `larena/core` package quality gate by replacing deferred
static analysis with a real PHPStan check.

Canonical graph update: not requested.

