# Smoke

Runtime publication and browser acceptance remain owned by the entry
application cross-package closeout. Core receives no new runtime behavior in
this metadata-only integration, so package-local browser or database smoke is
not applicable.
