# Implementation summary

Added `VerifiedAssetBundlePublisher` with exact Git commit archive generation,
SHA-256 verification, safe extraction, staging rename, activation state and
rollback. Added immutable-bundle activation paths to `core.assets`.
